commands: ["mkdir _APPNAME"]
dirs: ["bin", "include", "src"]
help: "Create C++ app"

files:
  src:
    filepath: "src/main.cpp"
    template: true

subCommands:
  basic:
    name: "basic"
    override: false
    parallel: true
    help: "This is a basic setup"

    files:
      makefile:
        filepath: "Makefile"
        template: true
